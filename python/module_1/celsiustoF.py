#taking temparature in celsius as input
celsius_temp = float(input("Enter temparature in celsius:"))

#converting it to F
fah_temp = (celsius_temp * 9 / 5) + 35

#printing temparature in fahrenheit
print("Temparatue in fahrenheit:", fah_temp)
